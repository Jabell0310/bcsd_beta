let userName = "Danny";
userName;
("23");
('I told her "Go away"');
let animal = "Pretty Puppy";
animal[0];
animal[7];
animal[99];
animal.length;
let firstName = Pep;
let lastName = Guardiola;
firstName + " " + lastName;
let result = 1 + "px";
result;
typeof result;
let loudWord = "hey are you there";
loudWord.toUpperCase();
let fixThisSpacewithmethod =
  "   You can do it because you made a great effort             ";
fixThisSpacewithmethod.trim();
let whisper = "IS THERE A MURDER?";
whisper.toLowerCase();
let findTheWord = "Find the word w";
findTheWord.indexOf("w");
findTheWord.indexOf("the");
findTheWord.indexOf("$");
let sliceMe = "Hey Hey Hey are you a frontend developer?";
sliceMe.slice(8);
sliceMe.slice(-33);
const changeMe = "skateboard";
let change = changeMe.slice(5).replace("o", "e");
change;
let repeatMe = "La";
repeatMe.repeat(10);
let demand = 5;
let price = 2000;
"Our customer`s demand is " +
  demand +
  ". So our company profit is " +
  demand *
    price`Our customer's demand is ${demand}. So our company profit is ${
      demand * price
    }``Hi back${6 + 3 + 2}`; //11
Math.E;
Math.PI;
Math.floor(23.9);
Math.ceil(21.1);
Math.random();
Math.floor(Math.random() * 5) + 1;
Math.pow(2, 3);
//문자열은 ''안에 텍스트나 숫자를 넣는 것을 말한다.
//문자열에 ""를 넣고 싶으면 바깥쪽 "와 다른 '를 넣어서 구현할 수 있다
//문자열 변수에 내장함수를 넣어서 글자 위치를 찾을 수 있고 문자 길이도 파악 가능하다
//문자와 문자끼리 숫자와 문자끼리 합성 가능하며 typeof로 입력했을 때 string이 나온다.
//문자열 안을 수정하거나 불러들일 수 있는 메서드를 쓸 수 있다.
//보통 문자열 출력은 36코드로 쓸 수 있지만 backtick을 쓰는 것이 더 편리하다.
//backtick은 ${} 안의 값을 계산한채로 출력한다.
//math도 primitive에 종류로 수학 연산이 가능하다.
//floor은 소수점이 5를 넘어도 버리고 ceil은 5를 넘지 않아도 반올림하는 속성이다.
